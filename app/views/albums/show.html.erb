<div class="catapimbas pt-5">
    <div class="espaco1"></div>
    <!-- Album/Artista -->
    <div class="container artista text-monospace text-light p-4">
        <div class="row">
            <div class="col-4 col-sm-3 col-md-2">
                <% if @album.photo.attached? %>
                    <td><%= image_tag @album.photo, class: 'card-img-top card-album', alt: 'Foto do album' %></td>
                <% else %>
                    <td><img src="https://encrypted-tbn0.gstatic.com/images?q=tbn%3AANd9GcQzkgiLkxtQFxJmZ6uq33CO-1dms_ROo1xl17wRXLs3JgBlQ-Wq&usqp=CAU" class="card-img-top card-album" alt="Foto padrão de album"></td>
                <% end %>
            </div>
            <div class="col-5 col-sm-6 col-md-7" >
                <h2>NAME: <%= @album.name %></h2>
                <h4>Artist: "<%= @album.artist.user.name %>"</h4>
            </div>
            <div class="col-3 col-sm-3" >
                <%= button_to "Ver    Albums", albums_path, method: :get, class: 'btn btn-outline-light btn-lg'  %>
                <%= button_to "Editar  Album", edit_album_path(@album), method: :get, class: 'btn btn-outline-light btn-lg'  %>
                <%= button_to 'Remover Album', album_path(@album), method: 'delete', data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-light btn-lg'  %>
            </div>
        </div>
    </div>
    <div class="espaco1"></div>
    <!-- Músicas -->
    <div class="container text-monospace playlist p-3">
        <table class="table text-light">
            <caption class="text-light"><%= link_to "Registrar nova musica", new_song_path, method: :get, style: 'color:#FFFFFF;' %></caption>
                <thead>
                    <tr class="">
                        <th scope="col" class="col-1"></th>
                        <th scope="col" class="col-3">Nome</th>
                        <th scope="col" class="col-4">Play</th>
                        <th scope="col" class="col-1">Editar</th>
                        <th scope="col" class="col-2">Apagar</th>
                    </tr>
                </thead>
                <tbody>
                <% Song.where(album_id:@album.id).each do |sp| %>
                    <tr>
                        <td scope="row"><i class="fa fa-music" aria-hidden="true"></i></td>
                        <td><%= sp.name %></td>
                        <td><-- tocador de música --></td>
                        <td><%= button_to "Editar Música", edit_song_path(sp), method: 'get', class: 'btn btn-outline-light' %></td>
                        <td><%= button_to "Apagar música", song_path(sp), method: 'delete', data: { confirm: 'Você tem certeza que quer apagar essa música?'}, class: 'btn btn-outline-light' %></td>
                    </tr>
                <% end %>
            </tbody>
        </table>
    </div>
    <div class="espaco1"></div>
</div>

<%
=begin%>
 <i class="fa fa-pencil" aria-hidden="true"></i>
<i class="fa fa-trash-o" aria-hidden="true"></i> 
<%
=end%>

<%
=begin%>
 
<%= button_to "<i class='fa fa-pencil'></i>".html_safe, edit_song_path(sp), method: 'get', class: "btn btn-success" %>

<%= button_to raw("<i class='fa fa-pencil'></i>"), edit_song_path(sp), method: 'get' %>  
 
<%
=end%>
